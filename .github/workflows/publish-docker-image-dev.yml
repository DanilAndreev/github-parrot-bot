name: Publish docker image to dev registry

on:
  push:
    branches: [ master ]

jobs:
  ghcr:
    runs-on:
      - skaffold
    steps:
      - name: Run Skaffold build and publish.
        run: skaffold run -p beta
