<b>{{webhook/repository}}</b> #{{info/tag}}
#pullrequest <i>{{state}}</i>

<b>{{info/title}}</b>
{{#if info/body}}
    {{info/body}}
{{/if}}

{{#if info/labels}}
    <b>Labels:</b>
    {{#each info/labels}}
        <code>{{name}}</code>
    {{/each}}
{{/if}}
{{#if info/assignees}}
    {{#if info/opened_by}}<b>Opened by:</b> {{#if info/opened_by/tag}}{{info/opened_by/tag}}{{else}}{{info/opened_by/login}}{{/if}}{{/if}}
    <b>Assignees:</b> {{#each info/assignees}}{{#if tag}}{{tag}}{{else}}{{login}}{{/if}} {{/each}}
{{/if}}
{{#if info/requested_reviewers}}
    <b>Requested review: </b>{{#each info/requested_reviewers}}{{#if tag}}{{tag}}{{else}}{{login}}{{/if}} {{/each}}
{{/if}}
{{#if info/milestone}}
    <b>Milestone</b> <i>{{info/milestone/title}}</i> {{#if info/milestone/due_on}}<code>{{info/milestone/due_on}}</code>{{/if}}
{{/if}}
{{#if checksuits}}
    {{#each checksuits}}
        ───────────
        <b>Suite:</b> <b><i>{{info/status}} {{info/conclusion}}</i></b>
        {{#if runs}}
            <i>Runs:</i>
            {{#each runs}}
                <b>{{info/name}}</b>: <i>{{info/status}}</i>
            {{/each}}
        {{/if}}
        {{#unless runs}}
            <i>Suite has no runs.</i>
        {{/unless}}
    {{/each}}
{{/if}}
